# HexGo dependencies — Python 3.12, CUDA 12.4
# Install with: "C:\Program Files\Python312\python.exe" -m pip install -r requirements.txt
# PyTorch must be installed separately (CUDA build):
#   pip install torch==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124

torch==2.6.0+cu124
numpy==2.4.3
fastapi==0.135.2
uvicorn==0.42.0
pydantic==2.12.5
matplotlib==3.10.8
pytest==9.0.2
